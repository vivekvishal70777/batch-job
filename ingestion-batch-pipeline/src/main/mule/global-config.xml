<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:secure-properties="http://www.mulesoft.org/schema/mule/secure-properties"
      xmlns:os="http://www.mulesoft.org/schema/mule/os"
      xmlns:sftp="http://www.mulesoft.org/schema/mule/sftp"
      xmlns:email="http://www.mulesoft.org/schema/mule/email"
      xmlns:db="http://www.mulesoft.org/schema/mule/db"
      xmlns:validation="http://www.mulesoft.org/schema/mule/validation"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/validation http://www.mulesoft.org/schema/mule/validation/current/mule-validation.xsd
        http://www.mulesoft.org/schema/mule/db http://www.mulesoft.org/schema/mule/db/current/mule-db.xsd
        http://www.mulesoft.org/schema/mule/email http://www.mulesoft.org/schema/mule/email/current/mule-email.xsd
        http://www.mulesoft.org/schema/mule/sftp http://www.mulesoft.org/schema/mule/sftp/current/mule-sftp.xsd
        http://www.mulesoft.org/schema/mule/os http://www.mulesoft.org/schema/mule/os/current/mule-os.xsd
        http://www.mulesoft.org/schema/mule/secure-properties http://www.mulesoft.org/schema/mule/secure-properties/current/mule-secure-properties.xsd">

    <global-property name="env" value="dev" doc:name="Default environment"/>
    <configuration-properties file="${env}-config.yaml" doc:name="Environment properties"/>
    <secure-properties:config name="Secure_Properties_Config"
                              file="${env}-secure-config.yaml"
                              key="${secure.key}"
                              doc:name="Secure Properties Config">
        <secure-properties:encrypt algorithm="Blowfish"/>
    </secure-properties:config>
    <configuration defaultErrorHandler-ref="global-error-handler" doc:name="Default error handler"/>

    <http:listener-config name="http-listener-config" basePath="${http.listener.base-path}" doc:name="HTTP Listener Config">
        <http:listener-connection host="${http.listener.host}" port="${http.listener.port}"/>
    </http:listener-config>

    <validation:config name="validation-config" doc:name="Validation Config"/>

    <db:config name="database-config" doc:name="SQL Server Config">
        <db:mssql-connection host="${database.sqlserver.host}"
                             port="${database.sqlserver.port}"
                             user="${database.sqlserver.user}"
                             password="${secure::database.sqlserver.password}"
                             databaseName="${database.sqlserver.name}">
            <reconnection>
                <reconnect frequency="2000" count="3"/>
            </reconnection>
            <db:pooling-profile maxPoolSize="${database.sqlserver.pool.max-size}"
                                minPoolSize="${database.sqlserver.pool.min-size}"
                                maxWait="${database.sqlserver.pool.max-wait-seconds}"
                                maxWaitUnit="SECONDS"
                                preparedStatementsCacheSize="20"/>
            <db:connection-properties>
                <db:connection-property key="encrypt" value="${database.sqlserver.encrypt}"/>
                <db:connection-property key="trustServerCertificate" value="${database.sqlserver.trust-server-certificate}"/>
                <db:connection-property key="loginTimeout" value="15"/>
            </db:connection-properties>
        </db:mssql-connection>
    </db:config>

    <sftp:config name="sftp-config" doc:name="SFTP Config">
        <sftp:connection host="${sftp.host}"
                         port="${sftp.port}"
                         username="${sftp.username}"
                         password="${secure::sftp.password}"
                         workingDir="${sftp.working-dir}">
            <reconnection>
                <reconnect frequency="3000" count="3"/>
            </reconnection>
        </sftp:connection>
    </sftp:config>

    <email:smtp-config name="email-smtp-config" doc:name="Email SMTP Config">
        <email:smtp-connection host="${email.smtp.host}"
                               port="${email.smtp.port}"
                               user="${email.smtp.user}"
                               password="${secure::email.smtp.password}">
            <email:properties>
                <email:property key="mail.smtp.starttls.enable" value="true"/>
                <email:property key="mail.smtp.starttls.required" value="${email.smtp.starttls-required}"/>
            </email:properties>
        </email:smtp-connection>
    </email:smtp-config>

    <os:config name="ObjectStore_Config" doc:name="Object Store Config"/>
    <os:object-store name="Migration_Lock_Store"
                     persistent="true"
                     maxEntries="10"
                     entryTtl="${migration.lock-ttl-minutes}"
                     entryTtlUnit="MINUTES"
                     expirationInterval="1"
                     expirationIntervalUnit="MINUTES"
                     config-ref="ObjectStore_Config"
                     doc:name="Migration lock store"/>
</mule>
