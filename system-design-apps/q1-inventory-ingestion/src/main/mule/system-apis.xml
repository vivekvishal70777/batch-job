<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:vm="http://www.mulesoft.org/schema/mule/vm"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/vm http://www.mulesoft.org/schema/mule/vm/current/mule-vm.xsd">

  <!-- System: analytics loader (Data Cloud stand-in). Independent SLA from SQL/OS inventory. -->
  <flow name="sys-analytics-loader">
    <vm:listener queueName="analytics-outbox" config-ref="VM_Config"/>
    <logger level="INFO" message="#['Data Cloud upsert sku=' ++ (payload.sku default '') ++ ' store=' ++ (payload.storeId default '')]"/>
  </flow>

  <!-- Email digest: on-error-continue in global handler so notify failure does not fail the job. -->
  <sub-flow name="sys-email-digest">
    <try>
      <logger level="INFO" message="#['EMAIL DIGEST job=' ++ vars.jobId ++ ' loaded=' ++ (vars.job.loaded default 0) ++ ' rejected=' ++ (vars.job.rejected default 0)]"/>
      <error-handler>
        <on-error-continue type="MULE:ANY">
          <logger level="ERROR" message="Email digest failed; job data already committed"/>
        </on-error-continue>
      </error-handler>
    </try>
  </sub-flow>
</mule>
