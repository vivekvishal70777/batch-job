<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:secure-properties="http://www.mulesoft.org/schema/mule/secure-properties"
      xmlns:ftp="http://www.mulesoft.org/schema/mule/ftp"
      xmlns:email="http://www.mulesoft.org/schema/mule/email"
      xmlns:db="http://www.mulesoft.org/schema/mule/db"
      xmlns:validation="http://www.mulesoft.org/schema/mule/validation"
      xmlns:file="http://www.mulesoft.org/schema/mule/file"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/file http://www.mulesoft.org/schema/mule/file/current/mule-file.xsd
        http://www.mulesoft.org/schema/mule/validation http://www.mulesoft.org/schema/mule/validation/current/mule-validation.xsd
        http://www.mulesoft.org/schema/mule/db http://www.mulesoft.org/schema/mule/db/current/mule-db.xsd
        http://www.mulesoft.org/schema/mule/email http://www.mulesoft.org/schema/mule/email/current/mule-email.xsd
        http://www.mulesoft.org/schema/mule/ftp http://www.mulesoft.org/schema/mule/ftp/current/mule-ftp.xsd
http://www.mulesoft.org/schema/mule/secure-properties http://www.mulesoft.org/schema/mule/secure-properties/current/mule-secure-properties.xsd">

    <configuration-properties file="${env}-config.yaml" />

    <http:listener-config name="http-listener-config" doc:name="HTTP Listener Config">
        <http:listener-connection host="${http.listener.host}" port="${http.listener.port}" />
    </http:listener-config>

    <validation:config name="validation-config" doc:name="Validation Config" />

    <db:config name="database-config" doc:name="Database Config">
        <db:mssql-connection host="${database.sqlserver.host}"
                             port="${database.sqlserver.port}"
                             user="${database.sqlserver.user}"
                             password="${secure::database.sqlserver.password}"
                             databaseName="${database.sqlserver.name}" />
    </db:config>

    <email:smtp-config name="email-smtp-config" doc:name="Email SMTP Config">
        <email:smtp-connection host="${email.smtp.host}"
                               port="${email.smtp.port}"
                               user="${email.smtp.user}"
                               password="${secure::email.smtp.password}">
            <email:properties>
                <email:property key="mail.smtp.starttls.required" value="${email.smtp.starttls-required}" />
            </email:properties>
        </email:smtp-connection>
    </email:smtp-config>

    <ftp:config name="sftp-config" doc:name="SFTP Config">
        <ftp:connection workingDir="${sftp.working-dir}"
                        host="${sftp.host}"
                        username="${sftp.username}"
                        password="${secure::sftp.password}" />
    </ftp:config>
	<global-property doc:name="Global Property" doc:id="fde454ba-ba79-4c74-add4-7fdbaab060ab" name="env" value="dev" />
	<configuration doc:name="Configuration" doc:id="e1e2c819-7507-4472-bb2c-51d2ce0dca32" defaultErrorHandler-ref="global-error-handler" />
	<configuration-properties doc:name="Configuration properties" doc:id="24d3fd8c-91a6-4913-9580-042523eb94ed" file="${env}-secure-config.yaml" />
	<secure-properties:config name="Secure_Properties_Config" doc:name="Secure Properties Config" doc:id="95e826fe-36ae-4ba5-a08a-635903771407" file="${env}-secure-config.yaml" key="${secure.key}" >
		<secure-properties:encrypt algorithm="Blowfish" />
	</secure-properties:config>

</mule>
