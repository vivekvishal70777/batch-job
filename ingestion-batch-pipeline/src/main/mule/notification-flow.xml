<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:email="http://www.mulesoft.org/schema/mule/email"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/email http://www.mulesoft.org/schema/mule/email/current/mule-email.xsd">

    <sub-flow name="send-email-sub-flow">
        <set-variable variableName="emailSubjectLine"
                      value="#[payload.headerLine default payload.message default 'Status']"
                      doc:name="Capture subject line"/>
        <choice doc:name="Select template">
            <when expression="#[payload.success == false]">
                <parse-template location="html/error-template.html" target="emailBody" doc:name="Error template"/>
            </when>
            <otherwise>
                <parse-template location="html/mail-template.html" target="emailBody" doc:name="Success template"/>
            </otherwise>
        </choice>
        <set-payload value="#[vars.emailBody]" doc:name="Use rendered HTML"/>
        <email:send config-ref="email-smtp-config"
                    fromAddress="${email.notification.from-address}"
                    subject="#[p('email.notification.subject-prefix') ++ ' - ' ++ vars.emailSubjectLine]"
                    doc:name="Send notification">
            <email:to-addresses>
                <email:to-address value="${email.notification.to-address}"/>
            </email:to-addresses>
            <email:body contentType="text/html"/>
        </email:send>
    </sub-flow>
</mule>
