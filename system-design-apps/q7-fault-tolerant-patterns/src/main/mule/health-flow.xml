<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

  <flow name="live-flow">
    <http:listener config-ref="http-listener-config" path="/live" allowedMethods="GET">
      <http:response statusCode="200"/>
    </http:listener>
    <set-payload value='#[output application/json --- { status: "UP", app: p("app.name") }]'/>
  </flow>

  <flow name="ready-flow">
    <http:listener config-ref="http-listener-config" path="/ready" allowedMethods="GET">
      <http:response statusCode="200"/>
    </http:listener>
    <set-payload value='#[output application/json --- { status: "READY", app: p("app.name") }]'/>
  </flow>
</mule>
