<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:db="http://www.mulesoft.org/schema/mule/db"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/db http://www.mulesoft.org/schema/mule/db/current/mule-db.xsd">

    <sub-flow name="execute-select-sub-flow">
        <logger level="DEBUG" doc:name="Start select"
                message="#[import * from dwl::common --- logLine(vars.jobId, 'execute-select-sub-flow', 'Start')]"/>
        <until-successful maxRetries="${retry.max}" millisBetweenRetries="${retry.interval-ms}" doc:name="Retry select">
            <db:select config-ref="database-config" doc:name="Select">
                <db:sql>#[vars.query]</db:sql>
                <db:input-parameters><![CDATA[#[vars.inputParameters default {}]]]></db:input-parameters>
            </db:select>
        </until-successful>
        <logger level="DEBUG" doc:name="End select"
                message="#[import * from dwl::common --- logLine(vars.jobId, 'execute-select-sub-flow', 'End')]"/>
    </sub-flow>

    <sub-flow name="execute-update-sub-flow">
        <logger level="DEBUG" doc:name="Start update"
                message="#[import * from dwl::common --- logLine(vars.jobId, 'execute-update-sub-flow', 'Start')]"/>
        <until-successful maxRetries="${retry.max}" millisBetweenRetries="${retry.interval-ms}" doc:name="Retry update">
            <db:update config-ref="database-config" doc:name="Update">
                <db:sql>#[vars.query]</db:sql>
                <db:input-parameters><![CDATA[#[vars.inputParameters default {}]]]></db:input-parameters>
            </db:update>
        </until-successful>
        <logger level="DEBUG" doc:name="End update"
                message="#[import * from dwl::common --- logLine(vars.jobId, 'execute-update-sub-flow', 'End')]"/>
    </sub-flow>

    <sub-flow name="bulk-write-sub-flow">
        <logger level="INFO" doc:name="Start bulk write"
                message="#[import * from dwl::common --- logLine(vars.jobId, 'bulk-write-sub-flow', 'Start')]"/>
        <until-successful maxRetries="${retry.max}" millisBetweenRetries="${retry.interval-ms}" doc:name="Retry bulk insert">
            <db:bulk-insert config-ref="database-config" doc:name="Bulk insert">
                <db:sql>#[vars.query]</db:sql>
            </db:bulk-insert>
        </until-successful>
        <logger level="INFO" doc:name="End bulk write"
                message="#[import * from dwl::common --- logLine(vars.jobId, 'bulk-write-sub-flow', 'End')]"/>
    </sub-flow>
</mule>
