<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:apikit="http://www.mulesoft.org/schema/mule/mule-apikit"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:os="http://www.mulesoft.org/schema/mule/os"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/mule-apikit http://www.mulesoft.org/schema/mule/mule-apikit/current/mule-apikit.xsd
        http://www.mulesoft.org/schema/mule/os http://www.mulesoft.org/schema/mule/os/current/mule-os.xsd">

    <global-property name="env" value="dev"/>
    <configuration-properties file="${env}-config.yaml"/>
    <configuration defaultErrorHandler-ref="prc-error-handler"/>

    <http:listener-config name="http-listener-config">
        <http:listener-connection host="${http.listener.host}" port="${http.listener.port}"/>
    </http:listener-config>

    <http:request-config name="sfdc-sys-http" basePath="${sfdc.sys.basePath}" responseTimeout="30000">
        <http:request-connection host="${sfdc.sys.host}" port="${sfdc.sys.port}"/>
    </http:request-config>

    <http:request-config name="erp-sys-http" basePath="${erp.sys.basePath}" responseTimeout="15000">
        <http:request-connection host="${erp.sys.host}" port="${erp.sys.port}"/>
    </http:request-config>

    <apikit:config name="customer-process-api-config" api="api/customer-process-api.raml"
                   outboundHeadersMapName="outboundHeaders" httpStatusVarName="httpStatus"/>

    <os:object-store name="idempotency-store" persistent="true" entryTtl="24" entryTtlUnit="HOURS" maxEntries="20000"/>
    <os:object-store name="customer-mapping-store" persistent="true" entryTtl="365" entryTtlUnit="DAYS" maxEntries="50000"/>
    <os:object-store name="cdc-replay-store" persistent="true" entryTtl="7" entryTtlUnit="DAYS" maxEntries="50000"/>
    <os:object-store name="bulk-watermark-store" persistent="true" entryTtl="90" entryTtlUnit="DAYS" maxEntries="10"/>
</mule>
