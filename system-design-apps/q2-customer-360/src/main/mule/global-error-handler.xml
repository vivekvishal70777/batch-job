<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

  <error-handler name="global-error-handler">
    <on-error-propagate type="HTTP:BAD_REQUEST,VALIDATION:*">
      <set-variable variableName="httpStatus" value="400"/>
      <set-payload value='#[output application/json --- {
        correlationId: correlationId,
        errorCode: "BAD_REQUEST",
        message: error.description default "Invalid request",
        timestamp: now() as String
      }]'/>
    </on-error-propagate>
    <on-error-propagate type="HTTP:NOT_FOUND,OS:KEY_NOT_FOUND">
      <set-variable variableName="httpStatus" value="404"/>
      <set-payload value='#[output application/json --- {
        correlationId: correlationId,
        errorCode: "NOT_FOUND",
        message: error.description default "Resource not found",
        timestamp: now() as String
      }]'/>
    </on-error-propagate>
    <on-error-continue type="EMAIL:*,CUSTOM:NOTIFY_FAILED">
      <logger level="ERROR" message="#['Notification failed jobId=' ++ (vars.jobId default '') ++ ' corr=' ++ correlationId]"/>
    </on-error-continue>
    <on-error-propagate type="MULE:ANY">
      <set-variable variableName="httpStatus" value="500"/>
      <logger level="ERROR" message="#['Unhandled ' ++ (error.errorType.identifier default 'UNKNOWN') ++ ' corr=' ++ correlationId]"/>
      <set-payload value='#[output application/json --- {
        correlationId: correlationId,
        errorCode: "INTERNAL_ERROR",
        message: "Unexpected error",
        timestamp: now() as String
      }]'/>
    </on-error-propagate>
  </error-handler>
</mule>
